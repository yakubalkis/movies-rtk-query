import { FormControl, InputLabel, Select, styled } from "@mui/material";

export const StyledSelect = styled(Select)({
  width: "250px",
  marginLeft: "20px",
});

export const StyledInputLabel = styled(InputLabel)({
  marginLeft: "20px",
});

export const StyledBox = styled(FormControl)({});
